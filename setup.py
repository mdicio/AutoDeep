from setuptools import setup, find_packages

setup(
    name="RealWTab",
    version="0.1",
    packages=find_packages(),
)

# This `setup()` function specifies the name and version of your package, and uses `find_packages()` to automatically discover and include all sub-packages in your project.
